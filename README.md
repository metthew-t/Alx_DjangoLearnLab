 # LibraryProject

This project is part of the ALX **Introduction to Django** module.  
It creates a simple Django project named `LibraryProject` to explore Django’s structure.

## Running the Project

1. Install Django:
   ```bash
   pip install django
